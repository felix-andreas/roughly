# Memory

Cross-session knowledge base for the agents building Roughly. Three horizons:

- **Short-term** — current focus and loose ends. Prune aggressively; delete each item once it is resolved or obvious from the tree.
- **Mid-term** — active priorities, open bugs, and technical debt. Lives across sessions until done.
- **Long-term** — durable, non-obvious design decisions and their rationale. Only things a future agent would otherwise rediscover. Keep terse and point at code or the docs.

Authoritative specs live in the docs site (`docs/src/content/docs/`), not here: `typing-reference.md` (the typing contract) + `type-checker.md` (the guide), `architecture.md`, `structure.md`, `testing.md`. Keep those current; keep this file for state and rationale.

## Short-term

- **Backlog #1 STRICT MODE (first slice) landed.** New `CheckConfig.strict` (default false); flows through `roughly/src/config.rs` for free (serde `[check] strict`). Semantics = Director's read (pending user confirm), recorded in `typing-reference.md` "Strict mode" section: strict flags a genuine **`Unknown` origin** (an `Unknown` first introduced into the lattice), tolerates `Any`, never flags propagation/error-recovery. **Origin sites** (engine, `typecheck.rs`): (O1) `ExpressionKind::Unsupported`; (O2) a Symbol whose **global-env-fallback** resolution is `Unknown` (base/stub binding with no known type — future-proof for library typing, rarely fires today). NOT origins: refs to a **local** binding (local path) or a **package-global** (`non_locals`+`global_bindings` path) that are `Unknown` (their *defining* site is the origin → no cross-file cascade), `non_locals`-but-not-global unresolved refs (naming reports D), unknown-name errors. **Cascade-avoidance:** origins emitted into `InferenceState.strict_origins` only at O1/O2 and only while `record_expression_types` is on (round 2); `check_module_with_naming` then **filters origins to those whose recorded resolved type is still `Unknown`** — drops `@trust Any` overrides (now `Any`) and anything under a failed top-level statement (errored exprs never recorded → no double-report). One root `Unknown` used downstream = one diagnostic (downstream uses are propagation, not origin sites). Diagnostics: distinct `DiagnosticCode::Strict` (`warning[strict]`), built in `analysis.rs::strict_origin_diagnostics` (binding wording when origin is an assignment value: "could not determine the type of `x`; add a type annotation"; else expression wording). Stored as `TypecheckDocumentOutput.strict_diagnostics` (computed always like type errors, **published only when `check_config.strict`** in `document_diagnostics`); `run_full` now runs typecheck when `typing || strict`. **Fixtures:** `tests/typecheck/strict/unknown_origins.R.test` (strict-ON via `run_strict_fixture` = multi-file diagnostics, strict+typing on): bare/binding origins, single-diagnostic cascade, no-double-report on a type error, `@trust Any` tolerated, cross-file (origin flagged once in a.R, ref in b.R not flagged); strict-OFF counterparts in `tests/diagnostics/strict_off.R.test`. **Open:** O2 has no fixture (untestable until base/library stubs bind names with `Unknown`); the "uninferred binding" directive bullet does not occur under current resolve semantics (free vars resolve to a type-variable, not `Unknown`) — noted in design, not papered over. **Did NOT touch** `Cargo.lock` (left dirty per guardrail), `format.rs`, `docs/` except `typing-reference.md`.
- **M1 (audit P0/quick-wins) landed** on `feat/hm-type-checker` as 8 green commits (`abf6f36`..`60b4096`). Each: own commit, `cargo test -p analysis` + `-p roughly` green, `cargo check --all-targets` clean. Items:
  - span→ExpressionId index (`Module.span_index`, O(arena)→O(log n)) + `HirArena::try_get` non-panicking IDE access.
  - UTF-16 positionEncoding negotiation (prefer utf-8, else utf-16); internal column = UTF-8 byte offset (tree-sitter Point); single conversion seam in `roughly/src/position.rs`; symbol/diagnostic paths routed through it (Item now carries byte-column `TextRange`).
  - resolve-error propagation in `check_compatibility` (no more `unwrap_or(Unknown)`); recursion-depth guards (inference `RECURSION_LIMIT=128`; type-syntax parser `TYPE_SYNTAX_RECURSION_LIMIT=160`; separate lowering guard for alias-body expansion). Native annotation overflow ≈200; ordering 128<141<160<200.
  - references/rename text prefilter (no persistent index) — find-refs@100k 218ms→67ms; provably equivalent (interner bijection over the same byte range).
  - completion cap 128 + `isIncomplete` (`CompletionResult`); inlay-hint viewport filter.
  - **`ROUGHLY_BLESS=1` auto-bless** for the fixture harness (rewrites `#++++` block bodies in place; idempotent on the real suite; leaves `#++++ any` alone). Use it for intentional expectation changes. Documented in `testing.md`.
- **M2 (type-core purity) landed** as 5 commits (`a15a36e`..`0d91700`). (1) `InferenceState` now has an ena-style snapshot/rollback undo log (`snapshot`/`commit`/`rollback_to`, `set_entry` chokepoint; reverses entries + rigid_variables + reclaims fresh ids; `recursion_depth` excluded; flat log so S4 margin unaffected). (2) `check_compatibility` is pure-on-failure: snapshot at the wrapper, commit on `Ok(true)`, rollback on `Ok(false)`/`Err` — a failed check leaks ZERO bindings (kills the old apply_annotation error-path unify); success keeps bindings (the `@new`/checked inference mechanism). (3) Principled nominal type-parameter variance (`Variance` lattice + `parameter_variances` occurrence walk): fn-param contravariant, fn-return/container/direct covariant, mixed→invariant, phantom(non-occurring)→bivariant(accept-any), nested-nominal args→conservative invariant. Closes the contravariant hole (`Handler<integer>` rejected where `T` contravariant). unify stays the invariant floor. (4) S5/S6 LSP test coverage; (5) the 3 resolve-`unwrap_or(Unknown)` sites documented as legitimate display/erasure fallbacks (not swallows). Contract in `typing-reference.md` (variance + mutable-container immutability assumption).
- **M3 (keystone reverse-dependency index) landed** as design `a261af0` + 4 slices (`e820329`..`bfeb0db`). (1) round-2 typecheck re-keyed on each doc's OWN `dependency_fingerprint` (rendered against the converged interface table) instead of one global env fingerprint — recompute COUNT now exact. (2) `reverse_dependencies: Symbol→{DocumentId}`, derived/patched from per-doc `non_locals` (keyed by attempted name; forward refs registered), with a debug full-rebuild drift assertion. (3) per-edit `dirty_documents` set + candidate selection (`dirty ∪ documents_referencing(changed_globals)`) bounding round-1 worklist + round-2 — O(package) scan → O(blast-radius). (4) `RecomputeReason` + `#!!!! recompute` fixture action → recompute scope fixture-visible (`recompute_scope.R.test` proves k+1). architecture.md "rechecks dependents" prose now true. **Blocker found+fixed in review:** winner-diff baseline must be `last_typecheck_global_bindings` (frozen at last typecheck), NOT live `package_naming_output` — intervening `resolve_package` (completion/refs/run_full) refreshes the live output without clearing dirty, which would erase a winner-change delta → stale referrer. **Contract precondition:** one-hop dirtying is correct only because inference never flows across files (re-exports handled by the two-round fixed-point). Stale prose in architecture.md is now corrected.
- **Stub-framework design proposal** drafted (architect + researcher); see `docs/.../stdlib-stubs.md` (design note, implementation sequenced after M3/M4). Reuse `#:` decl-only `.R` stub files with a body-skipping loader; ONE source of truth for scheme-expressible stdlib + a small irreducible operator/`c`/`list` kernel; stubs are immutable high-durability inputs kept OUT of the reverse-dep index/dependency fingerprint; needs type-syntax extensions (variadics, dotted param names like `na.rm`, generic `T[]` suffix). Closes the `T`/`F`/`pi` MEMORY gap as the first increment.
- **Interface fixed-point round cap was a latent correctness bug (fixed).** The old `const MAX_PACKAGE_INTERFACE_ROUNDS = 32` truncated round 1: propagation advances exactly one hop per round (each round's table = previous round's output), so any acyclic re-export/forward-ref chain DEEPER than 32 never converged — and the round-2 all-docs candidate fallback could NOT repair it because round 2 reads the same unconverged `final_table` (the deep schemes stay `Unknown`/stale; even the initial full check was wrong). Verified empirically. Fix: bound the round-1 loop by `package_naming.global_bindings.len() + 8`, NOT by document count — measured, a 2-doc/3-global package needs 4 rounds, so a `doc_count+1` bound false-fires. Correct bound rests on monotonicity: every *acyclic* package-global's exported scheme transitions at most once (Unknown→concrete; leaves are stable annotations/literals), so synchronous iteration converges in ≤ `#globals + 1` rounds; the `+8` is slack so the assert can't false-fire. (Re-export *cycles* are non-monotone — they oscillate — and are handled separately by the oscillation guard that pins cycle members to `Unknown`; see the RESOLVED cyclic-re-export note below.) Legitimate structures always converge (the 40-link deep-chain test: docs=42, exactly 42 rounds, well under bound); common case still breaks early when the worklist empties, so no perf change except a pathological deep-chain O(N²) worst case. Kept `converged` tracking + all-docs round-2 fallback (release-safe, never stale) + `debug_assert!(converged, ...)` (fires in debug/test builds): with the proven bound, non-convergence can only mean a real fixed-point defect (e.g. a non-monotone scheme) = a coherence failure, so it fails loudly in tests while the fallback keeps release builds correct. Test `deep_re_export_chain_past_round_cap_is_not_left_stale` (40-link chain) proves the tail recomputes to the real `character` error after flipping `g`, not a stale clean result. architecture.md updated.
- **M4.2 (incremental package naming) acceptance fixtures landed.** `tests/naming/global/incremental_naming.R.test` (6 multi-gen cases) is the explicit edge-case proof for the M4.2 slice: per-generation it asserts the resolved winner (`Symbol(name@bN)` binds the live package winner via `global_naming_result.global_bindings`) AND the exact package-naming diagnostics (A overwrite chains, D could-not-resolve). Cases: `winner_flip` (later-path definer starts/stops → winner + overwrite warnings move), `drop` (path-last definer drops → falls to next-latest), `delete` (sole definer deleted → name leaves global_bindings, referrer reports D), `earlier_path_insert` (earlier definer becomes loser, winner unchanged), `rename_re_add` (old-name referrers lose resolution, new resolves), `D_defined_ness_flip_on_non_dirty_referrer` (KEY: file A adds/drops `g`; file B — restated with identical content so it is NON-dirty — sees its D diagnostic disappear then reappear via the M3 reverse-dep hop `documents_referencing(N)`, both directions). The `run_naming_global_fixture` runner was generalized to apply per-generation create/edit/delete/move ops and render a snapshot each gen; `resolve_package`'s debug drift assertion runs every generation → these also prove incremental==full-rebuild for free.
- **M4.3 (incremental package type index) landed.** Type side now mirrors the value side: `type_definitions: Symbol→DocumentId→Vec<TypeInfo>` candidate index (source of truth), materialized `package_type_index` + `duplicate_type_names`, and `type_references` reverse-dep index — all patched per re-derived/deleted doc. Single membership rule `document_type_definitions(module)` + single winner/duplicate rule `apply_type_definition_outcome`, both shared by the incremental patch AND the `build_type_index` oracle. `maintain_package_naming` no longer calls `build_type_index`/`package_document_ids` — it re-collates only `naming_dirty_type_names` and routes (C)/(T) per affected type name (co-definers ∪ `documents_type_referencing`), replacing the old `type_index_changed → all-docs` fallback. Drift assertions extended (`assert_type_definitions_consistent` rebuilds candidate+materialized from primary modules; `assert_type_references_consistent` folds `document_type_references`) — fire every `resolve_package`. Fixtures: `tests/naming/global/incremental_types.R.test` (added/dropped/duplicate-flip, kind-flip + arity-flip on NON-dirty referrer, forward type ref). NB type names `Pair` is a reserved builtin (type_syntax.rs arity-2) — don't use it in fixtures.
- **PERF reality check:** the M4.3 background claim that `build_type_index` was "the dominant ~87ms residual" is WRONG for `benchmark_single_file_recheck_in_large_package`: `common::generate_file` emits ZERO `@type`/`@alias` defs, so `build_type_index` processed nothing. Benchmark unchanged by M4.3 (76→77ms, noise). Timing breakdown of the 77ms body-edit recheck: **`build_package_interface_table` ~62ms** (round1 fixed-point 43ms + final_table 19ms) — the M3 VALUE interface table, rebuilt O(package-globals=~60k) per round — plus winner-diff 8ms. THAT is the real next bottleneck for flat recheck (out of M4.3 scope): make the package interface table incremental (patch only changed winners' exports) instead of a full O(package-globals) rebuild each round.
- **M4.3 remaining follow-up (separable):** `typecheck`'s `type_definitions_fingerprint` is still package-global → any type-def change forces ALL docs into the round-2 candidate set. Tighten to a per-doc type-dependency fingerprint (render the type defs each doc references via `document_type_references`, route through `documents_type_referencing`), analog of the M3 value `dependency_fingerprint`. Needs a frozen `last_typecheck_type_index` baseline (analog of `last_typecheck_global_bindings`) because intervening `resolve_package` refreshes the materialized type index without clearing `dirty_documents`. Independent of the now-incremental naming type index.
- **M4.2/M4.3 polish slice landed (bare-block globals + single-source predicate + soak).** (1) **Bare-block false-positive fixed:** `document_package_definitions` now recurses into bare top-level `{ }` Blocks (incl. nested bare blocks), so `{ x <- 1L }` at top level makes `x` a package global; `if`/`for`/`while`/function bodies stay excluded (they are `If`/`For`/`While`/`Function`, not `Block`, so the recursion never reaches them) — conditional-global tier still deferred. The same bare-block recursion was added to `find_exported_binding` (production: ide/typecheck export lookup), which previously only scanned direct top-level `Assign`s and would have failed to find a block-bound global even though `global_bindings` listed it. (2) **Single source of truth:** `build_candidate_order` now calls `document_package_definitions` (was a byte-identical copy of the membership loop) — the membership rule exists exactly once, so incremental and oracle can't drift together (the drift assertion is blind to the rule itself). (3) Fixtures: `cross_file_values.R.test::bare_block_globals__bare_top_level_block_assign_is_package_global` (resolves cross-file → `Symbol(x@b0)`, no warning) and `failures.R.test::unknown_values__conditional_top_level_block_assign_is_not_package_global` (`if (TRUE) { x <- 1L }` → `Symbol(x@?)` + could-not-resolve). (4) **Seeded soak** `test_incremental.rs::seeded_soak_incremental_matches_full_rebuild`: fixed xorshift seed, 3000 add/edit/delete ops over 6 paths / small name+type pools (plain/bare-block/conditional assigns, re-exports, `@type`/`@alias`, type refs, typed-fn interfaces), `resolve_package` after each op fires all five drift assertions (reverse-deps, value+type candidate indexes, materialized type index, four-category naming oracle). ~0.84s, replaces the old unverifiable "180k calls" claim.
- **Release-path incremental verification now exists (was the "no-regret" gap below).** The five drift assertions (`assert_reverse_dependencies_consistent` / `_package_definitions_` / `_type_references_` / `_type_definitions_` / `_package_naming_consistent`) and their two call-site blocks in `analysis.rs` are now gated `#[cfg(any(debug_assertions, feature = "verify-incremental"))]` instead of `#[cfg(debug_assertions)]`, so the optimized RELEASE incremental path is verifiable. New additive empty feature `analysis/verify-incremental` (+ passthrough `roughly/verify-incremental` so root `cargo --features` resolves). Run: `just verify-incremental` = `cargo test -p analysis --release --features verify-incremental --test test_incremental`. Liveness proven (clearing `reverse_dependencies` before the asserts panics the release+feature soak at `analysis.rs:1504`; reverted). Default `cargo test -p analysis` (debug) unchanged — `debug_assertions` still enables all five. The `debug_assert!(converged)` convergence guard is intentionally NOT in this set (separate guard, release-covered by the all-docs fallback). **Soak broadened** with three bug-class interleavings (rolls over `below(10)`): edit→IDE-`hover`→edit (intervening read drives `resolve_package` between mutations; rolls 1..=2), add→delete→re-add same path (roll 6), package↔script move flipping `non_package_documents` membership (roll 5). Slots now track `LiveDocument { is_script, source }`; `slot_path(i, is_script)` = `R/fi.R` vs `scripts/fi.R`. Still fixed-seed, ~3s debug full file.
- **RESOLVED — cyclic typed re-exports are legitimate; the fixed-point now absorbs them.** Root cause (diagnosed, not (i) alpha-renaming): a mutual re-export cycle (`a <- b`; `b <- a`) is a genuine **period-2 oscillation**, not monotone — the table is rebuilt from the previous round's outputs (Jacobi iteration), and on a pure re-export 2-cycle `a` and `d` *swap* values every round (`a_N = table[d]_{N-1}`, `d_N = table[a]_{N-1}`), so when they start unequal (the stale concrete-vs-Unknown start an incremental edit produces) they never settle. The old comment's "names on a genuine cycle stay `Unknown` forever" was false. Per-round trace confirmed: `a: fn(value: integer) -> integer` ↔ `a: Unknown` flipping every round. **Fix (`analysis.rs` interface fixed-point loop):** an **oscillation guard** — track per-symbol the sequence of table renderings (only for names whose winner doc is a candidate that round); a name whose rendering *returns to an earlier value while differing from the previous round* is on a cycle → **pin it to `Unknown`** (`pinned_unknown` set forces every subsequent table, incl. `final_table`, to `Unknown` for that symbol and enqueues its referrers). The cycle collapses to `Unknown` and the framework is monotone again, so the loop converges within the bound and `debug_assert!(converged)` stays valid. **Deep-chain guard preserved:** an acyclic name's rendering only transitions away from `Unknown` once and never returns, so it is never pinned; a genuinely truncated deep chain still fails to converge and trips the assert (`deep_re_export_chain_past_round_cap_is_not_left_stale` green). Also a release perf win: cyclic edits no longer spin the full round bound. Coverage: fixtures `values.R.test::reexport_cycles__*` (2-cycle + 3-cycle resolve to `Unknown`, typed fn alongside still checks); the **seeded soak now drives `typecheck` every step** and asserts diagnostics match a from-scratch `run_full` every 10 steps (`soak_full_rebuild_diagnostics`), exercising the cycle handling under churn (~2.6s).
- Known wording quirk (pre-existing): the parser recursion diagnostic renders as `error[syntax-error] Syntax Error: ...` (doubled prefix) — same as other annotation-syntax errors; tracked under the diagnostic-wording mid-term item.
- Steering docs are agents-first: single `.agents/memory/MEMORY.md`; authoritative specs in the docs site; human notes in `.local/` (untracked).
- **DX track (formatter): insta→fixture migration + `#:` annotation formatting landed.** (1) Formatter tests now run on the fixture harness: `tests/format/{base,special,misc,annotations}.R.test` via `run_fixture_suite("tests/format", ...)` in `roughly/tests/test_format.rs`; `documentation_examples` is a golden-file check against `docs/.../formatter.md` (bless `ROUGHLY_BLESS=1`); all `test_format__*.snap` deleted. `insta` stays only for `roughly/tests/test_tree.rs`. (2) `#:` type-annotation comments are reformatted to canonical surface-type spacing by a **token-level** normalizer in `format.rs` (`format_type_annotation_comment`/`tokenize_annotation`/`annotation_space_between`), NOT by round-tripping `analysis::type_syntax::render_*` — the renderer is lossy for a formatter (collapses expanded `@param`/`@return` blocks into compact `fn(...)`, and reorders mixed positional/named `fn` params). The normalizer is line-local + non-invasive: preserves token order, identifier casing, and line breaks; bails to verbatim on any byte outside the annotation alphabet (dotted names, URLs, prose, non-ASCII idents). Mirrors `render_surface_type` spacing exactly. Idempotence pinned by `annotation_formatting_is_idempotent` (format(expected)==expected over all annotation fixtures). **No analysis-crate API needed.** Deferred (flagged for edge-case review #5): continuation-line indentation of multi-line `#:` blocks collapses to one space after `#:` (no block-aware alignment/wrapping yet).
- **DX track (formatter edge-case review #5) landed.** Adversarial probe (panic + idempotence sweep, then manual output inspection) found ONE severe semantics-altering defect: **raw string literals were destroyed** — the `kind::STRING` arm assumed `"`+content+`"`, so `r"(...)"`/`R"[...]"` lost their delimiters and (with no tree-sitter content field) collapsed to `""` (e.g. `x <- r"(a\b)"` → `x <- ""`). It was idempotent and panic-free, so only eyeballing caught it. Fix in `format.rs`: a string node whose first char is `r`/`R` is emitted byte-for-byte (raw strings are the only string nodes that start with a letter). Fixtures: `base.R.test::literals__strings_raw` (incl. embedded quotes, backslashes, dash-bracket delims, multi-line raw). Everything else probed clean (deep nesting 3000 parens / 2000 calls — no stack overflow; unicode idents/strings, CRLF, comments-in-awkward-positions, quote normalization incl. raw, switch fallthrough — all correct/idempotent). Documented limitations pinned as fixtures: `annotation_multiline_compact__indented_continuation_is_left_aligned` (the deferred indent-collapse) and `annotation_preserve__double_colon_is_spaced` (`#: a::b` → `#: a:: b`; `::` is not surface-type grammar so this only affects already-invalid annotations). Added `annotation_inline__*` (trailing `#:` after code is reformatted). Docs: raw-string preservation noted in the String Literals section.
- **DX track (website #7) landed.** The marketing landing page is `docs/src/pages/index.astro` (a standalone Astro page — distinct from the Starlight technical docs under `docs/src/content/docs/`, which are the CTO's lane). It is ~3.3k lines: all marketing COPY is in lines 1-597; the rest is inline CSS + a hero particle-field `<canvas>` script (leave that alone). Adversarial marketing review (sub-agent) verdict: the genuine world-first — first static type checker for R — was buried under generic "modern developer tooling for R"; pain and audience were never named; "300k lines" was the wrong hero proof point. Applied copy-only edits (no structure/animation change): H1 now "The first type checker for R.", subhead/eyebrow/title/meta re-led on the type checker + non-invasive promise, named the audience ("R users who have outgrown RStudio"), de-jargoned ("Hindley-Milner"/"over LSP" → benefit wording), demoted 300k to a reassurance, and foregrounded the one-click VS Code install over `cargo install` (R users lack a Rust toolchain). Kept claims honest (type checker is opt-in, checks "most" code). Build: `cd docs && npx astro build` (green); `dist/` is git-ignored. Full critique + punch-list saved nowhere external — see commit. Roadmap: #6 (docs world-class pass) is the remaining DX item, deferred until the CTO's `typing-reference.md`/`architecture.md` churn settles to avoid lane collision.
- **DX track (docs world-class pass #6) — non-typing pages landed.** Lane-scoped: did `getting-started.mdx`, `linter.md`, `language-server.mdx`, `development.md`, `structure.md`, `testing.md`; SKIPPED the CTO's live lane (`typing-reference.md`, `architecture.md`, `type-checker.md`, `configuration.md`) per coordinator — still owed once the CTO is quiescent. Adversarial reviewer + source verification found the dominant issue: shipped flagship features were mislabeled experimental/future across pages, contradicting the product. Verified against source and fixed: `typing`/`unused`/`goto`/`refs`/`rename`/`hovering` are STABILIZED (cli.rs warns if passed as experimental flags); the only real experimental flag is `range_formatting`; type *inference* is on by default (powers hover/inlay), type-error + unused diagnostics are opt-in via `roughly.toml [check] {typing,unused}=true`. Also: install story corrected (VS Code Marketplace one-click first, Zed extension exists, prebuilt binaries on Releases for linux/macos/windows — NOT crates.io, so no `cargo install`); language-server Roadmap that re-listed shipped goto-def/refs/rename as "future" deleted; development.md gained a build/test guide (was absent); structure.md synced (`types.rs`, `stdlib.rs`/`stdlib_base.R` added). Commits `4c0cd5c`, `11476e5`, `951aa6a`. `cd docs && npx astro build` green per commit; `dist/` git-ignored. ONE flag for the CTO: `language-server.mdx` now marks undefined-variable diagnostics "🧪 experimental" (was "missing") on the basis that strict mode landed (`d155d01`) — CTO should confirm/refine when typing pages are released. NOTE: the website (#7, committed `c9dbdb2`) still says "Zed soon" but the Zed extension now exists — easy follow-up fix.

- **Backlog #2 LT2 perf gate + base-fn breadth landed (this session).** (1) `Analysis::new` now delegates to a private `build(base_path, lint_config, check_config, interner, stub_library)`; new `pub fn new_with_stub_library(...)` injects a caller-supplied `StubLibrary` (builds its own fresh interner — safe because the only caller passes `StubLibrary::empty()`, which has no symbols to mis-resolve). This is the injection point for the bench. (2) `tests/test_incremental.rs::benchmark_stub_library_zero_per_edit_cost` (`#[ignore]`, run via `just verify-incremental` is NOT it — run `cargo test -p analysis --release --test test_incremental -- --ignored --nocapture`). Two scenarios × {`StubLibrary::load`, `empty`}, 20 averaged rounds, 500-file pkg. **Result: presence is zero-cost** — plain sources (no base refs) measured ≈ −4% (noise); base-referencing sources ≈ +20%, which is *legitimate inference work* (resolving base names to real schemes; the empty baseline skips it by yielding `Unknown`), NOT graph/bookkeeping overhead. Both recheck exactly 1 doc. New `common/mod.rs` generators `generate_package_with_base_refs`/`generate_file_with_base_refs`. (3) Base corpus widened in `stdlib_base.R`: precise `is.na`→logical, `as.integer`/`as.character`/`as.numeric`, `substr`(3-arity)→character; `Any` degrades for `min`/`max`/`mean`/`substring`/`gsub`/`sub` (optional/variadic args the grammar can't express). Fixtures in `tests/typecheck/stdlib/base.R.test` (28 cases). (4) `docs/.../stdlib-stubs.md` gained §7 CRAN tier (per-project discovery via renv/DESCRIPTION/`.libPaths()`, introspection-generated shallow stubs `getNamespaceExports()`+`formals()`→arity+arg-names+`Any`/`Incomplete` returns, cached per version, curated overrides, unstubbed→`Any`), §8 R-version keying (embedded corpus selected by detected/configured R version), §9 future-slice sequencing (introspection generator → stubtest LT5 → `NamespaceGet` for `pkg::name`) flagged as **needs a real R install — not buildable in this env**. CRAN/introspected stubs are the same immutable high-durability input kind as the embedded corpus, just generated.

## Mid-term

Active priorities and debt:

- **ARCHITECTURE FORK — OPEN, user's call: hand-rolled vs. memoized-query framework (salsa).** Full analysis (two independent expert reviews, quality-first / effort-excluded) in `.agents/decisions/incremental-architecture-and-recheck.md`. Summary: (A) recheck trigger DECIDED — tiered debounced live-as-you-type + force-full on save (Roughly is the only R checker, so save-only is wrong); (B) framework-vs-hand-rolled DEFERRED to the user (experts split; quality-first leans framework via de-risked phased migration; tripwires = cross-file inference / `::`+`library()` namespace edges [the stub framework trips this] / cross-file dynamic dispatch); (C) the value-interface-table slice is PAUSED — necessary only at 300k, subsumed if a framework is adopted, so do NOT hand-build it next. **No-regret action — DONE (release verification):** the drift-oracle + seeded soak now run in RELEASE behind the additive `verify-incremental` feature (`just verify-incremental`); see the short-term release-path-verification note. Still TODO: wire `just verify-incremental` into CI. Carry-risk: the one-hop premise is enforced by nothing at compile time; violating it auto-triggers the framework decision.
- **Incremental recheck: M3 done — the interface fixed-point + round-2 typecheck are now O(blast-radius), not O(package).** Per-doc dependency re-keying + reverse-dependency index + dirty-set candidate selection (commits `e820329`..`bfeb0db`; exit proof in `recompute_scope.R.test`). **Remaining O(package) cost:** `resolve_package`/`rebuild_package_naming` still rebuilds the global binding table on every `package_version` bump (≈111ms single-file recheck @500 files). Next step: incremental package naming — diff a doc's exported names against its previous version and patch `global_bindings`, re-running winner selection only for affected names (guard shadowing winner logic with a focused fixture). Also: `type_definitions_fingerprint` is still package-global (a type-def change falls back to all-docs in round-2) — tighten to per-doc type deps later.
- **Find-references / rename / completion: M1 cheap wins landed; structural reuse still pending.** A text prefilter cut find-refs@100k 218ms→67ms (commit `31be96d`) and completion is now capped at 128 + `isIncomplete` (`b93ba77`). Remaining cost is the per-document tree walk across all docs — the deeper fix is the **reverse-dependency index** (package-global Symbol → referring DocumentIds) feeding both refs/rename and the interface fixed-point. That index is the M-series keystone (also unblocks per-doc round-2 typecheck keying). goto-definition is already O(1)-file.
- **Type-syntax error ranges are coarse.** `SurfaceType` carries no per-node ranges, so unresolved-type / annotation errors underline the whole annotation. Thread ranges through type-syntax parsing so `list{age: intgr}` underlines only `intgr`.
- **Tree-sitter access is string-based in the `analysis` front end** (`kind()`/`child_by_field_name()`), while `roughly` uses `kind_id()`/`field_id()`. Consolidate on id-based matching; dedupe the rope/tree helpers and the AST-walking symbol indexer shared with `roughly` (`roughly/src/index.rs`).
- `resolve_document` public phase entry + edit-time orchestration.
- `typecheck/project` follow-ups: package-winner behavior with conflicting types; `Collate` coverage once the fixture harness models `DESCRIPTION`.

Open typing soundness / quality gaps:

- **RESOLVED (M2.3, `e7ae7a4`):** nominal type-argument variance is now computed per parameter from its occurrence in the representation and applied in the `check_compatibility` Nominal arm (contravariant args flip, invariant require both directions). The `Handler<integer>` contravariant unsoundness is closed. **Deferred refinement:** nested generic-nominal arguments (`Outer<T> = Sink<T>`) are treated *conservatively invariant* — sound but over-restrictive; a precise variance COMPOSITION (compose outer polarity with the inner nominal's own per-parameter variance, with cycle handling) would re-admit sound nested covariant cases.
- **S4:** find-references / rename for S4 names need a use-site index; `@` slot access still lowers to `Unsupported` (needs a `Slot` HIR node + lowering/typing); slots-as-class-children in the outline.
- **Structural constraints on inferred params:** `function(x) x$name` / `x[[1L]]` / `c(x, 1L)` leak `type1` or error instead of constraining `x` to a record / indexable / atomic. Needs row/shape constraints on inference variables, analogous to the numeric constraint.
- `T` / `F` base bindings (need a base-environment model); vectorized `&` / `|` (need a semantics decision).
- Diagnostic wording: alias-cycle not reported on unused declarations; `@if-unknown`-on-known wording; annotation-semantic errors render under `syntax-error` with a doubled `Syntax Error:` prefix.

## Long-term

Durable design decisions and non-obvious facts (point to code/docs; do not re-derive):

- **Incremental at document grain (two-round interface model).** Per-document local naming, then a package-level dependency-cached interface fixed-point. Generalization is level-based. Cross-file references are scheme-based — no inference flow across files. Each document's interface is cached on a dependency fingerprint of the schemes it references; the fixed-point only re-derives changed documents (but still scans all to compare — see mid-term). Authoritative detail on the docs architecture page.
- **Typing core.** HM inference with union-find inference variables. Numeric constraint (`types::Constraint`) carried on `Unbound` entries and quantified in `TypeScheme`; `function(x) x + 1L` → `<T: numeric> fn(x: T) -> T`. A numeric var that escapes a binding without being abstracted by a function parameter defaults to `double`; defaulting is level-gated (`default_free_numeric`) so a local inside a polymorphic function does not monomorphize the enclosing param.
- **Polymorphic annotations are enforced via rigid (skolem) variables.** A `<T>` binder lowers to a rigid var (`rigid_variables` map in `InferenceState`, `fresh_rigid_variable`) that refuses to be bound or constrained while the body is checked, then generalizes back to `<T>`; instantiation at call sites uses ordinary fresh vars. Checks in `bind_variable` / `unify_variables` / `constrain_type`; rendered by declared name via `display_with_rigid_names`. Function-annotation checking is unified in `infer_function_expression` (return focused-checked for a clean message, then whole-signature for parameter shape).
- **Compatibility rules.** Structural (record/tuple) compatibility is checked covariantly per element/field and unifies variables (lets `@new` / checked annotations infer through inference-variable fields). Function types are contravariant in parameters, covariant in returns. Return position is checked with compatibility, not equality. if/else unifies its branches (`NULL` → nullable, `Unknown` → `Unknown`, otherwise unify).
- **Type-error surfacing is decoupled from the phase.** The typecheck phase runs lazily for the typing IDE features (hover types, inlay hints, signature help) — all on by default. Type-error *diagnostics* are gated behind `[check] typing = true` (default off): `document_diagnostics` and proactive `run_full` only include/run them when set. `Analysis::type_errors_enabled()`. `[check] unused = true` gates unused-local warnings the same way.
- **Search-like IDE features share `ide::search_match`:** subsequence matching, smart-case (any uppercase in the query ⇒ case-sensitive), queries shorter than 3 chars are prefix-only, ranked by `MatchScore` (exact/prefix/substring/subsequence tier, then first-match position). Workspace symbols collect → rank → truncate(128); completion reuses it; reserved keywords stay prefix-only.
- **S4 navigation is unified into `ide::symbol_occurrences_at`.** Identifier resolution first; when the cursor is on a string literal, a structural S4 scan (`s4_symbol_at` / `collect_s4_occurrences`) resolves class/generic names in `setClass`/`setGeneric`/`setMethod`/`new`. goto-def, find-references, and rename all work cross-file through this one path.
- **Unused-local detection.** Naming tags each binding with a `BindingKind`; `unused_bindings` are local assignments no symbol use resolves to. Excludes parameters, for-loop variables, top-level/exported bindings, and `.`/`_`-prefixed names.
- **Index/field-access diagnostics are dedicated** (`field b does not exist`, `position N does not exist`, `expected a list`, `cannot index … without a statically known position/field name`, `[ is not supported on …`) with whole-access ranges.
- **Error handling: coherence failures panic.** LSP document-sync / analysis-sync failures are unrecoverable and panic immediately. But IDE feature lookups must never panic — the typecheck Symbol arm falls back to `Unknown` for a not-yet-bound local (forward / recursive / conditionally-defined / inside an uninferred subtree).
- **Graduated, always-on:** hover, find-references, rename, plus the typing IDE features above. Document symbols intentionally stay AST-based in `roughly` for the per-keystroke path.
- **Benchmarks.** `just bench` builds synthetic 10k/100k/200k packages (`crates/analysis/tests/common/mod.rs`, `test_benchmark.rs`): cold full check, single-file recheck, and IDE-feature latency (`benchmark_ide_100k/200k`).
