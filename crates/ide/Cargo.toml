[package]
name = "ide"
edition = "2024"
version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
path = "src/ide.rs"

[dependencies]
semantics = { path = "../semantics" }
syntax = { path = "../syntax" }
