# Curated snapshot of real `base` package exports (a HAND-MAINTAINED list, NOT R-derived).
#
# Purpose: the exhaustiveness check asserts every name the `base.Rtypes` stub corpus declares appears
# here (so a stubbed name that is not a real export — a typo — fails), and reports how many names here are
# not yet stubbed (a coverage gauge, never a failure). This list is therefore a SUPERSET of the stubbed
# names: it holds the stubbed names plus a curated selection of common, definitely-real base exports that
# are not stubbed yet. It is deliberately small and is not the full ~1200-name base namespace; signature
# validation (arity/types) needs a real R and is a separate future slice.
#
# Blank lines and `#` comments are ignored.

# --- stubbed ---
F
Filter
Find
Map
Position
Reduce
T
abs
as.character
as.integer
as.logical
as.numeric
cat
ceiling
endsWith
floor
format
grep
grepl
gsub
is.character
is.function
is.list
is.logical
is.na
is.null
is.numeric
lapply
length
local
mapply
max
mean
min
nchar
paste
paste0
pi
prod
range
regmatches
rep
rev
round
sapply
seq
seq_along
seq_len
signif
sort
sprintf
sqrt
startsWith
strsplit
sub
substr
substring
sum
tolower
toupper
trimws
trunc
unique
vapply

# --- real exports not yet stubbed (coverage gauge) ---
Negate
Recall
Vectorize
all
any
append
apply
array
as.factor
as.list
as.vector
attr
attributes
cumsum
diff
dim
do.call
exists
factor
gettextf
identical
ifelse
inherits
integer
list
match
match.arg
matrix
names
nrow
ncol
numeric
order
rowSums
colSums
setdiff
setNames
stopifnot
switch
table
tabulate
tryCatch
union
unlist
vector
which
