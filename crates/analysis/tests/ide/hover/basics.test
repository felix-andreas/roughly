#==== symbols
#---- local_parameter_use
#---- R/main.R
value <- function(parameter) parameter
#++++ any
#!!!! hover parameter.hover
R/main.R:1:30
#++++
### Lowering

```text
Symbol(parameter)
```

---

### Naming

```text
local resolution: binding `parameter` at R/main.R:1:19
```

#==== nesting
#---- call_targets
#---- R/main.R
identity <- function(argument) argument
value <- 1L
result <- identity(value)
#++++ any
#!!!! hover assign.hover
R/main.R:3:1
#++++
### Lowering

```text
Assign(result)
```

---

### Naming

```text
local resolution: binding `result` at R/main.R:3:1
```
#!!!! hover callee.hover
R/main.R:3:12
#++++
### Lowering

```text
Symbol(identity)
```

---

### Naming

```text
local resolution: unresolved `identity`
package resolution: binding `identity` at R/main.R:1:1
```
#!!!! hover paren.hover
R/main.R:3:19
#++++
### Lowering

```text
Call(arguments: 1)
```
#!!!! hover argument.hover
R/main.R:3:20
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:2:1
```

#---- binary_operator_targets
#---- R/main.R
value <- 1L
other <- 2L
result <- value + other
#++++ any
#!!!! hover operator.hover
R/main.R:3:17
#++++
### Lowering

```text
Symbol(+)
```

---

### Naming

```text
local resolution: unresolved `+`
```
#!!!! hover left.hover
R/main.R:3:12
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```
#!!!! hover right.hover
R/main.R:3:20
#++++
### Lowering

```text
Symbol(other)
```

---

### Naming

```text
local resolution: unresolved `other`
package resolution: binding `other` at R/main.R:2:1
```

#---- subset_targets
#---- R/main.R
data <- 1L
index <- 1L
result <- data[index]
#++++ any
#!!!! hover container.hover
R/main.R:3:15
#++++
### Lowering

```text
Subset(arguments: 1)
```
#!!!! hover value.hover
R/main.R:3:12
#++++
### Lowering

```text
Symbol(data)
```

---

### Naming

```text
local resolution: unresolved `data`
package resolution: binding `data` at R/main.R:1:1
```
#!!!! hover argument.hover
R/main.R:3:17
#++++
### Lowering

```text
Symbol(index)
```

---

### Naming

```text
local resolution: unresolved `index`
package resolution: binding `index` at R/main.R:2:1
```

#---- subset2_targets
#---- R/main.R
data <- 1L
index <- 1L
result <- data[[index]]
#++++ any
#!!!! hover container.hover
R/main.R:3:15
#++++
### Lowering

```text
Subset2(arguments: 1)
```
#!!!! hover value.hover
R/main.R:3:12
#++++
### Lowering

```text
Symbol(data)
```

---

### Naming

```text
local resolution: unresolved `data`
package resolution: binding `data` at R/main.R:1:1
```
#!!!! hover argument.hover
R/main.R:3:18
#++++
### Lowering

```text
Symbol(index)
```

---

### Naming

```text
local resolution: unresolved `index`
package resolution: binding `index` at R/main.R:2:1
```

#---- dollar_targets
#---- R/main.R
data <- 1L
result <- data$field
#++++ any
#!!!! hover operator.hover
R/main.R:2:15
#++++
### Lowering

```text
Dollar(field)
```
#!!!! hover value.hover
R/main.R:2:12
#++++
### Lowering

```text
Symbol(data)
```

---

### Naming

```text
local resolution: unresolved `data`
package resolution: binding `data` at R/main.R:1:1
```
#!!!! hover name.hover
R/main.R:2:17
#++++
### Lowering

```text
Dollar(field)
```

#---- block_and_unary_targets
#---- R/main.R
value <- 1L
block <- { value }
negated <- -value
#++++ any
#!!!! hover block.hover
R/main.R:2:10
#++++
### Lowering

```text
Block(expressions: 1, trailing_semicolon: false)
```
#!!!! hover block_inner.hover
R/main.R:2:13
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```
#!!!! hover unary.hover
R/main.R:3:12
#++++
### Lowering

```text
UnaryMinus
```
#!!!! hover unary_inner.hover
R/main.R:3:14
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```

#---- control_flow_targets
#---- R/main.R
value <- 1L
items <- 2L
chooser <- if (TRUE) value else 0L
result <- for (item in items) value
loop <- while (TRUE) value
again <- repeat value
#++++ any
#!!!! hover if.hover
R/main.R:3:12
#++++
### Lowering

```text
If(alternative: true)
```
#!!!! hover condition.hover
R/main.R:3:17
#++++
### Lowering

```text
Logical(true)
```
#!!!! hover consequence.hover
R/main.R:3:23
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```
#!!!! hover alternative.hover
R/main.R:3:34
#++++
### Lowering

```text
Integer(0L)
```
#!!!! hover for.hover
R/main.R:4:11
#++++
### Lowering

```text
For(item)
```
#!!!! hover sequence.hover
R/main.R:4:25
#++++
### Lowering

```text
Symbol(items)
```

---

### Naming

```text
local resolution: unresolved `items`
package resolution: binding `items` at R/main.R:2:1
```
#!!!! hover body.hover
R/main.R:4:32
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```
#!!!! hover while.hover
R/main.R:5:10
#++++
### Lowering

```text
While
```
#!!!! hover while_condition.hover
R/main.R:5:17
#++++
### Lowering

```text
Logical(true)
```
#!!!! hover while_body.hover
R/main.R:5:23
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```
#!!!! hover repeat.hover
R/main.R:6:10
#++++
### Lowering

```text
Repeat
```
#!!!! hover repeat_body.hover
R/main.R:6:18
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/main.R:1:1
```

#---- function_parameter_sites
#---- R/main.R
value <- function(parameter) parameter
#++++ any
#!!!! hover keyword.hover
R/main.R:1:10
#++++
### Lowering

```text
Function(parameter)
```
#!!!! hover binding.hover
R/main.R:1:19
#++++
### Lowering

```text
Function(parameter)
```
#!!!! hover use.hover
R/main.R:1:31
#++++
### Lowering

```text
Symbol(parameter)
```

---

### Naming

```text
local resolution: binding `parameter` at R/main.R:1:19
```

#==== workspace
#---- cross_file_use_updates_after_move
#---- R/a.R
value <- 1L
#++++ any
#---- R/b.R
result <- value
#++++ any
#!!!! hover lookup.hover
R/b.R:1:11
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/a.R:1:1
```
#.... after_move
#---- move R/a.R -> R/c.R
#!!!! hover lookup.hover
R/b.R:1:11
#++++
### Lowering

```text
Symbol(value)
```

---

### Naming

```text
local resolution: unresolved `value`
package resolution: binding `value` at R/c.R:1:1
```
