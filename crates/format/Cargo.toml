[package]
name = "format"
edition = "2024"
version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
path = "src/format.rs"

[dependencies]
syntax = { path = "../syntax" }

[dev-dependencies]
syntax = { path = "../syntax" }
