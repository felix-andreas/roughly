[package]
name = "differential"
edition = "2024"
version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
path = "src/differential.rs"

[dependencies]
analysis-legacy = { path = "../analysis-legacy" }
semantics = { path = "../semantics" }
syntax = { path = "../syntax" }

[dev-dependencies]
salsa.workspace = true
format = { path = "../format" }
ide = { path = "../ide" }
ropey.workspace = true
roughly-legacy = { path = "../roughly-legacy" }
syntax = { path = "../syntax" }
