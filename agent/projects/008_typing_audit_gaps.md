# 008 Typing Audit Gaps [in-progress]

Generated by the `typing-test-audit` workflow (11 dimensions). 557 green fixture cases landed under `tests/**/audit_*.R.test`; the gaps below are cases where current behavior is not rust-analyzer-quality. Fix top-down; delete entries as they are fixed and add a green fixture.

Total: 57 gaps found. Landed green: 557.

## Fixed (this session)

- `@new` (and checked annotations) now infer through inference-variable fields via covariant
  record/tuple compatibility — `Instrument <- function(id, name) { #: @new Instrument; list(id =
  id, name = name) }` infers `fn(id: integer, name: character) -> Instrument` (plain and generic).
- Reserved constants type correctly: `NA` (logical), `NA_integer_`/`NA_real_`/`NA_complex_`/
  `NA_character_`, `Inf`/`NaN` (double), `1i` (complex).
- `c(...)` follows R's coercion hierarchy (`logical<integer<double<complex<character`) and drops
  `NULL`, so `c(1L, NA)` → `integer[]`, `c(1L, "a")` → `character[]`, `c(x, NULL)` → `c(x)`.
- (earlier) record types reject duplicate fields and allow a trailing comma; expression-level
  annotations are applied.
- **Index/field-access diagnostics (errors + operators dimensions).** `[[`, `$`, and `[` now emit
  dedicated, range-precise diagnostics instead of the generic `index_type_mismatch`: "field `b` does
  not exist in `C`", "position 5 does not exist in `C`", "expected a list, found `T`", "cannot index
  `C` without a statically known position/field name", "`[` is not supported on `T`". Whole-access
  ranges. New `InferenceError` variants + `SubscriptKind`. Green fixtures in
  `tests/diagnostics/audit_errors.R.test` and the indexing bindings suites.
- **Cross-file re-export interfaces (was 3 blockers + 3 high).** The interface phase is now a
  dependency-cached package-level fixed-point, so `second <- first`, `get_base <- function() base`,
  and deep cross-file chains resolve and propagate type errors. Green fixtures in
  `tests/typecheck/project/values.R.test`.

## Top remaining clusters (prioritized)

1. **Structural constraints on inferred parameters (high).** `function(x) x$name`, `x[[1L]]`,
   `c(x, 1L)` error/leak `type1` instead of constraining `x` to a record/indexable/atomic. Needs
   structural (row/shape) constraints on inference variables, analogous to the numeric constraint.
3. **Diagnostic wording/ranges (errors dimension).** Remaining: alias-cycle not always reported on
   unused declarations; `@if-unknown`-on-known wording; the `error[syntax-error] Syntax Error:
   invalid semantics:` doubled prefix + wrong category for annotation-semantic errors. (Index/field/
   `$` wording + ranges are now dedicated diagnostics — see Fixed.)
4. **`T`/`F` base bindings and vectorized `&`/`|`** — need a base-environment model and a semantics
   decision respectively.

The remaining per-dimension gap detail follows.


## atomics (5 gaps, 19 green)

- **[high]** `T  (also F)`
  - actual: error: unknown name
  - desired: logical
  - R base binds T and F to TRUE/FALSE. Checker reports unknown name. Very common in real R code. Fix: seed base bindings T->logical, F->logical (they are rebindable variables, not reserved literals, so model as default base-env logical bindings rather than literals).
- **[high]** `NA`
  - actual: Unknown
  - desired: logical
  - NA is the logical NA constant in R; a bare NA should infer as logical. Checker yields Unknown. TYPING_SEMANTICS lists logical as an atomic name but never specifies NA constants. Fix: lower NA reserved constant to a logical literal; also add to TYPING_SEMANTICS atomic-literal section.
- **[high]** `NA_integer_ (also NA_real_, NA_character_)`
  - actual: Unknown
  - desired: integer (resp. double, character)
  - Typed NA constants are reserved literals in R with fixed types. Checker yields Unknown for all three. Fix: lower NA_integer_/NA_real_/NA_character_ as typed literals of integer/double/character.
- **[medium]** `Inf (also NaN)`
  - actual: Unknown
  - desired: double
  - Inf and NaN are reserved double constants in R. Checker yields Unknown. Fix: lower Inf/NaN as double literals.
- **[medium]** `1i (also 0i)`
  - actual: Unknown
  - desired: complex
  - Imaginary literals are complex in R; complex is a documented atomic name in TYPING_SEMANTICS. Literal inference for complex is missing (yields Unknown) even though complex arithmetic/comparison is intentionally unsupported. Fix: lower imaginary numeric literal token to a complex literal type; litera

## vectors (9 gaps, 51 green)

- **[high]** `1i`
  - actual: Unknown
  - desired: complex
  - Complex literals are not typed. `complex` is a documented atomic name (TYPING_SEMANTICS Atomic names) but the lexer/lowering does not classify imaginary literals, so they fall to Unknown. Fix: recognize complex numeric literals in literal typing. This also breaks c(1i, 2i) (errors instead of complex
- **[high]** `c(1i, 2i)`
  - actual: error: type mismatch
  - desired: complex[]
  - Downstream of complex-literal gap: c() sees Unknown operands and rejects them. Once complex literals type correctly, c(...) over complex should yield complex[] per the Combine rule.
- **[high]** `NA`
  - actual: Unknown
  - desired: logical
  - Bare NA is a logical scalar in R and is pervasive. Checker infers Unknown. Fix: type NA as scalar `logical` in literal typing. Cascades: c(1L, NA) errors instead of integer[]; any NA in vector construction/arithmetic mistyped.
- **[high]** `c(1L, NA)`
  - actual: error: type mismatch
  - desired: integer[]
  - Downstream of NA gap: NA being Unknown makes c() reject the mix. With NA typed as logical and the integer/logical NA-promotion handled, this should be integer[] (R promotes logical NA into the numeric vector).
- **[medium]** `NA_integer_`
  - actual: Unknown
  - desired: integer
  - Typed-NA sentinels (NA_integer_, NA_real_, NA_character_) have a fixed atomic type and should infer that scalar type. Currently Unknown.
- **[medium]** `c(1L, NULL)`
  - actual: error: type mismatch
  - desired: integer[]
  - R drops NULL inside c() (c(x, NULL) is the idiomatic conditional-append). TYPING_SEMANTICS only says args must be atomic vectors and NULL is incompatible with everything, so current behavior is contract-consistent but not R-quality. Either special-case NULL as droppable in c() or document the reject
- **[medium]** `TRUE & FALSE`
  - actual: Unknown
  - desired: logical
  - Vectorized logical operators `&` and `|` are not modeled (only `&&`/`||` are in TYPING_SEMANTICS). They are extremely common in R. Desired: elementwise logical with the standard scalar/array shape rule (scalar&scalar -> logical, otherwise logical[]). Needs a semantics decision plus operator support;
- **[medium]** `c(TRUE, FALSE) | c(TRUE, TRUE)`
  - actual: Unknown
  - desired: logical[]
  - Same vectorized `|`/`&` gap on array operands; should yield logical[].
- **[low]** `c(NULL)`
  - actual: error: type mismatch
  - desired: NULL
  - c(NULL) is NULL in R. Same root as c(1L, NULL): NULL args should be dropped, and an all-NULL combine yields NULL (matching the c() no-arg rule).

## lists (4 gaps, 60 green)

- **[medium]** `#: list{a: list[integer]}\n value <- list(a = list(1L, 2L))\n value`
  - actual: error: type mismatch
  - desired: list{a: list[integer]}
list{a: list[integer]}
  - Coercion is not applied to field values inside a fixed-shape record-like annotation. Field `a` has inferred type list{integer, integer} (tuple-like) and the annotation expects list[integer]; the tuple->array coercion that works at the top level (see passing nested_tuple_coerce_to_nested_array) is no
- **[medium]** `#: list{a: list[named: integer]}\n value <- list(a = list(x = 1L, y = 2L))\n value`
  - actual: error: type mismatch
  - desired: list{a: list[named: integer]}
list{a: list[named: integer]}
  - Same root cause as the tuple-field case: record-like field value (here list{x: integer, y: integer}) does not get the record->map coercion that the contract allows and that works at top level (see passing record_coerce_to_map_annotation). Fixed-shape field positions require exact match instead of co
- **[low]** `list(a = 1L, a = 2L)`
  - actual: list{a: integer, a: integer}
  - desired: likely a duplicate-field diagnostic, or at minimum a defined disambiguation rule for `$a`/`[["a"]]` access
  - Duplicate field names are silently kept in the record-like type. R allows this at runtime (first match wins for `$`), but a record type with two identical field names has ambiguous name-based [[ access semantics and no diagnostic. Contract is silent; rust-analyzer-quality would likely warn. Left out
- **[low]** `list(function(x) x)`
  - actual: list{fn(x: ?1) -> ?1}
  - desired: a user-facing rendering that does not expose the raw internal inference-variable id (e.g. list{fn(x: _) -> _} or a generalized form)
  - In expression position the inner function is not at a binding boundary so it is not generalized; the type still renders the internal inference variable `?1`. This leaks an implementation-detail id into user-facing output. Behavior (no generalization) is contract-consistent, but the `?1` surface form

## nominal_new (3 gaps, 40 green)

- **[blocker]** `#: @type Instrument {list{id: integer, name: character}}\n \n Instrument <- function(id, name) {\n   #: @new Instrument\n   list(id = id, name = name)\n }`
  - actual: error[type-error] expected `list{id: integer, name: character}`, found `list{id: type1, name: type2}`  (binding fails to typecheck)
  - desired: Instrument: fn(id: integer, name: character) -> Instrument
  - Core constructor idiom. @new against a RECORD/structural representation does not unify the constructed value's field inference variables against the representation field types. The unbound param metavars (id, name) stay unresolved (rendered type1/type2) and the compatibility check reports a mismatch
- **[blocker]** `#: @type Box<T> {list{value: T}}\n \n makeBox <- function(value) {\n   #: @new Box<integer>\n   list(value = value)\n }`
  - actual: error[type-error] expected `list{value: integer}`, found `list{value: type1}`
  - desired: makeBox: fn(value: integer) -> Box<integer>
  - Same root cause as the record case but for a generic nominal: even a single-field record representation fails to unify the field metavar. Generic @new (with explicit type args) plus structural representation should still drive param inference through field unification.
- **[medium]** `#: @type Pair {list{name: character, age: double}}\n \n #: @new Pair\n x <- list(name = "bob", age = 20)`
  - actual: x: list{name: character, age: double}  (no diagnostic; nominal NOT applied)
  - desired: either x: Pair (honor the user @type) or a diagnostic that `Pair` is a reserved builtin generic name
  - `Pair` is hardcoded as a builtin generic expecting 2 args (crates/analysis/src/type_syntax.rs:402). A user `#: @type Pair {...}` plus `#: @new Pair` silently degrades to the representation type with NO diagnostic, because `@new Pair` resolves to the builtin generic (0 of 2 args) yet emits no error h

## function_inference (7 gaps, 46 green)

- **[high]** `get_name <- function(x) x$name`
  - actual: error[type-error] expected `type1`, found `Unknown`
--> 1:25-1:31
  - desired: Should not leak the internal type-variable name `type1`. rust-analyzer-quality: either infer a record/field constraint on x (no row polymorphism today, so likel
  - Record/list field access on an unconstrained inferred parameter forces a concrete record type that the param can't have. Diagnostic renders the raw inference var `type1`, which is a user-facing leak. Fix needs either a structural/record constraint mechanism for params or, minimally, a non-leaking me
- **[high]** `firstel <- function(x) x[[1L]]`
  - actual: error[type-error] expected `type1`, found `integer`
--> 1:27-1:29
  - desired: `[[` on an inferred param should constrain x to an indexable shape (list/vector) and infer the element type, or fall back to Unknown without erroring; must not 
  - Same root cause as field access: no way to express an indexable constraint on a fresh param var, so `[[` errors and the message leaks the internal var name.
- **[high]** `addone <- function(x) c(x, 1L)`
  - actual: error[type-error] expected `integer`, found `type1`
--> 1:25-1:26
  - desired: `c(x, 1L)` should constrain x to an atomic vector compatible with integer (numeric), inferring something like `<T: numeric> fn(x: T) -> T[]`, not error. Must no
  - c() requires atomic args; an unconstrained param var is treated as a rigid expected type instead of being constrained by the call. Needs c() to push a numeric/atomic constraint onto flexible operands the way arithmetic does.
- **[medium]** `loopf <- function(n) loopf(n)`
  - actual: diagnostics suite: `No diagnostics.`; bindings suite: runner cannot render the scheme (`error: unknown name`)
  - desired: Self-recursive top-level functions should typecheck and produce a renderable scheme, e.g. `loopf: <T> fn(n: T) -> Never` (or `Unknown`). A name should resolve t
  - Inconsistent: diagnostics path reports nothing while the bindings path hits an internal `unknown name` computing loopf's own scheme (cyclic self-reference during inference). The binding's own scheme is not visible to its RHS, breaking recursion.
- **[medium]** `withdef2 <- function(x, y = x) y`
  - actual: withdef2: <T, U> fn(x: T, [y]: U) -> U
  - desired: withdef2: <T> fn(x: T, [y]: T) -> T
  - The default value `y = x` (type T) is not unified/linked with the inferred type of parameter y (U). The result generalizes x and y independently, so calling withdef2(1L) with y omitted yields an unresolved/free return type. Default-value compatibility against the inferred param type must constrain i
- **[low]** `rangef <- function(n) 1L:n`
  - actual: rangef: <T: numeric> fn(n: T) -> integer[]
  - desired: Result element type should track n's resolved type: `integer[]` when n is integer, `double[]` when n is double. The current monomorphic `integer[]` is unsound f
  - `:` with a flexible numeric operand hardcodes the integer-result branch. The type system can't express a result that depends on T's concrete numeric type, so it picks integer[] unconditionally; a defensible alternative is `double[]`, but neither is correct for both calls.
- **[low]** `cmp <- function(x, y) x > y`
  - actual: cmp: <T, U> fn(x: T, y: U) -> logical
  - desired: x and y should be constrained to share a comparison family, so cross-family calls like `cmp("a", 1L)` are rejected. With only same-type unification available th
  - Comparing two unconstrained params leaves both fully free, so the contract's same-family requirement is unenforced and unsound cross-family calls type-check. Rooted in the documented absence of a comparison-family constraint mechanism.

## numeric (2 gaps, 76 green)

- **[medium]** `f <- function(x) { y <- x + 1L; x == "a" }   (symmetric: f <- function(x) { y <- x == "a"; x + 1L })`
  - actual: No diagnostics. (binding infers f: <T: numeric> fn(x: T) -> T / logical)
  - desired: Cross-family comparison error: x is numeric-constrained (via x + 1L) and is compared against character "a"; per the Comparison operators contract this is a type
  - Root cause: there is no character/logical inference constraint, so comparison against a non-numeric family `leaves the operand unconstrained` and never rechecks once the operand is otherwise pinned to numeric. A flexible operand that is (or becomes) numeric and is compared against character silently
- **[low]** `f <- function(n) 1L:n   (also: f <- function(x) x:10L)`
  - actual: f: <T: numeric> fn(n: T) -> integer[]
  - desired: Result element type must track the numeric var, not assume integer. When the flexible operand instantiates to double the range result is double[] (contract: `x:
  - Range `:` lowering picks integer[] whenever an operand is an unresolved numeric inference var, ignoring that the var may resolve to double. Corner case (range over a polymorphic numeric param), but the inferred scheme is unsound for double callers. Fix: when a `:` operand is a still-flexible numeric

## operators_indexing (6 gaps, 64 green)

- **[high]** `#: list[named: integer]\n v <- list(foo = 1L)\n i <- "foo"\n v[[i]]`
  - actual: error: type mismatch
  - desired: integer | NULL
  - Contract defines map-like list[named: T] name-based `[[` as returning `T | NULL` with NO static-literal restriction (only record-like/tuple-like require literal indices). The checker wrongly applies the record literal-name requirement to map-like lists. Fix: in the list `[[` handler, for map-like sh
- **[high]** `#: list[integer]\n v <- list(1L, 2L)\n v$foo`
  - actual: error: type mismatch
  - desired: integer
  - `$name` is documented sugar for `[["name"]]`. On the same array-like list `v[["foo"]]` returns `integer`, but `v$foo` errors. The `$` path does not treat array-like-list name access identically to `[[`. Fix: route `$`/backtick name access through the same list-`[[` name handling so array-like lists 
- **[medium]** `list(foo = 1L, bar = "x")$`foo``
  - actual: error: type mismatch
  - desired: integer
  - Contract: "Backtick-quoted names follow the same rule" as `$name`. The backtick-quoted field name is not extracted as a literal field key, so record-field lookup fails. Fix: lower a backtick-quoted `$` segment to the same literal-name key used for plain identifiers.
- **[medium]** `#: @type Pair {list{integer, character}}\n \n #: @new Pair\n p <- list(1L, "x")\n p`
  - actual: list{integer, character}
  - desired: Pair
  - `@new` on a nominal whose representation is tuple-like `list{...}` does not wrap the value in the nominal; record-like, map-like, array-like, and atomic representations all preserve the nominal name. The @new path appears to special-case structural list shapes and returns the representation instead 
- **[low]** `2i < 3i`
  - actual: Unknown
  - desired: error: invalid operand (complex not supported in comparison)
  - Complex literals like `2i` are not typed as `complex`; they infer as `Unknown`, so comparison silently propagates `Unknown` instead of rejecting per "complex and raw operands are not supported". Requires typing complex literals first; then the comparison family check rejects complex.
- **[low]** `#: list[integer]\n v <- list(1L, 2L)\n v[]`
  - actual: error: function arity mismatch
  - desired: list[integer] (whole-container identity), or at least an indexing-specific diagnostic
  - `[` is modeled as a function call, so empty `v[]` leaks "function arity mismatch". R returns the whole object for empty `[`. Either support empty `[` returning the same container type, or emit an indexing-specific message instead of the call-arity wording.

## control_flow (2 gaps, 57 green)

- **[high]** `flag <- TRUE\n x <- if (flag) 1L\n if (flag) 1L else x   # x: integer | NULL`
  - actual: error: type mismatch
  - desired: integer | NULL
  - if-else branch unification only special-cases a branch that is EXACTLY NULL. When one branch is T and the other is the nullable form T | NULL they are neither equal nor NULL, so it errors. This breaks the very common `if (a) x else if (b) y` idiom (the trailing `else if` is an if-without-else of typ
- **[low]** `if (TRUE) 1L else c(1L, 2L)`
  - actual: error: type mismatch
  - desired: integer[]
  - Both branches are integer, differing only in vector shape (scalar vs array). The system already models scalar->array coercion (T into T[]), so the branches could unify to integer[]. The contract (TYPING_SEMANTICS if...else: 'does not use any additional coercion beyond the nullable-union rule') delib

## annotations (2 gaps, 63 green)

- **[high]** `#: @forall T\n #: @param {T} value\n #: @return {character}\n bad <- function(value) value\n \n (also compact: #: <T> fn(value: T) -> character\n bad <- function(value) value)`
  - actual: No diagnostics.
  - desired: error[type-error] expected `character`, found `T`
--> 4:1-4:28
| bad <- function(value) value
  - A polymorphic/rigid annotated return type is not checked against the function body. The non-forall variant IS checked correctly (`#: @param {integer} count / @return {character} / function(count) count` errors `expected character, found integer`), and the compact `<T> fn(value:T) -> character` form 
- **[high]** `#: fn(a: integer, b: integer) -> integer\n f <- function(a) a\n \n (also expanded: #: @param {integer} a / #: @param {integer} b / #: @return {integer}\n f <- function(a) a)`
  - actual: error[type-error] This call passes 1 positional argument(s), but the function accepts 2.
--> 2:1-2:19
| f <- function(a) a
  - desired: error[type-error] expected `fn(a: integer, b: integer) -> integer`, found `fn(a: integer) -> integer`
--> 2:1-2:19
| f <- function(a) a
  - When an annotation declares more parameters than the function definition has, the annotation-vs-definition compatibility check reuses the call-arity diagnostic and renders a misleading 'This call passes N positional argument(s)' message even though there is no call site. It should render a definitio

## errors (10 gaps, 56 green)

- **[high]** `x <- list(1L, 2L)\n x[[5L]]`
  - actual: error[type-error] expected `list{integer, integer}`, found `integer`
--> 2:4-2:6
| x[[5L]]
  - desired: error[type-error] position 5 does not exist in `list{integer, integer}`
--> 2:1-2:8
  - Out-of-range literal positional `[[` on a tuple-like list. The checker reports a nonsensical 'expected list, found integer' by unifying the index literal against the container instead of running a dedicated position-existence check. TYPING_SEMANTICS requires the literal position to exist; missing po
- **[high]** `x <- list(a = 1L)\n x$b`
  - actual: error[type-error] expected `list{}`, found `Unknown`
--> 2:1-2:4
| x$b
  - desired: error[type-error] field `b` does not exist in `list{a: integer}`
--> 2:1-2:4
  - Name-based `[[`/`$` on a record-like list for a non-existent literal field. Message 'expected list{} found Unknown' is meaningless to a user. Field-existence on fixed-shape records needs a dedicated diagnostic naming the missing field and the record type, per TYPING_SEMANTICS.
- **[high]** `x <- 1L\n x$foo`
  - actual: error[type-error] expected `integer`, found `Unknown`
--> 2:1-2:6
| x$foo
  - desired: error[type-error] expected a list, found `integer`
--> 2:1-2:6
  - `$`/`[[` on a non-list scalar. Very common mistake. Current message 'expected integer found Unknown' is backwards and confusing; should say the operand must be a list (or list-like) and report the found atomic type.
- **[medium]** `x <- list(1L, 2L)\n i <- 1L\n x[[i]]`
  - actual: error[type-error] expected `list{integer, integer}`, found `integer`
--> 3:4-3:5
| x[[i]]
  - desired: error[type-error] cannot index `list{integer, integer}` without a statically known position
--> 3:1-3:7
  - Non-literal index on a tuple-like list. The error correctly fires, but the wording (unifying index var against the list) does not explain that tuple `[[` requires a statically known literal position. Range points at the index var rather than the whole access.
- **[medium]** `x <- c(1L, 2L)\n x[1L]`
  - actual: error[type-error] expected `integer[]`, found `integer`
--> 2:3-2:5
| x[1L]
  - desired: error[type-error] `[` is not supported on `integer[]`
--> 2:1-2:6  (or infer Unknown without a misleading error)
  - `[` on a vector is explicitly undefined in TYPING_SEMANTICS. Instead of a clear 'unsupported operator' message or graceful Unknown, the checker emits a confusing 'expected integer[] found integer' from unifying the index against the vector.
- **[medium]** `#: @alias A {B}\n #: @alias B {A}\n value <- 1L`
  - actual: No diagnostics.
  - desired: error[type-error] Type alias `A` expands in a cycle. (reported at the declaration)
  - Mutual alias cycle is only detected when an alias is actually USED in an annotation (then it reports 'Type alias `A` expands in a cycle.' at the use site). An unused cyclic alias declaration silently passes. TYPING_SEMANTICS states alias definition cycles are errors, so the cycle should be diagnosed
- **[medium]** `#: @if-unknown integer\n value <- 1L`
  - actual: error[type-error] expected `Unknown`, found `integer`
--> 2:1-2:12
| value <- 1L
  - desired: error[type-error] `@if-unknown` only applies when the inferred type is `Unknown`, but the type is already known to be `integer`
--> 2:1-2:12
  - @if-unknown used on a value whose type is already known. The error fires (correct) but reuses the generic mismatch renderer ('expected Unknown found integer'), which leaks the internal Unknown sentinel and does not explain the @if-unknown rule. Needs a dedicated annotation diagnostic.
- **[medium]** `c(list(1L), 2L)`
  - actual: error[type-error] expected `integer`, found `list{integer}`
--> 1:3-1:11
| c(list(1L), 2L)
  - desired: error[type-error] expected an atomic vector, found `list{integer}`
--> 1:3-1:11
  - `c(...)` requires atomic arguments; lists are unsupported. The current message 'expected integer' misattributes the element type chosen from a later argument and obscures the real rule (no lists in c()). Range is correct.
- **[low]** `f <- function() 1L\n for (item in f) { }`
  - actual: error[type-error] expected `integer[]`, found `fn() -> integer`
--> 2:1-2:20
| for (item in f) { }
  - desired: error[type-error] cannot iterate over `fn() -> integer`
--> 2:14-2:15  (point at the iteration source)
  - `for` over a non-iterable. Error correctly fires but 'expected integer[]' picks an arbitrary element shape and the range spans the whole loop instead of the iteration source. A 'cannot iterate over T' message anchored at the source would be clearer.
- **[low]** `#: @new Missing\n value <- list(name = "bob")  (and other annotation-semantic errors: @new on alias, generic arg-count, duplicate @forall, duplicate type name)`
  - actual: error[syntax-error] Syntax Error: invalid semantics: ...
  - desired: error[type-error] ... (or at least drop the redundant `Syntax Error:` prefix)
  - Annotation/semantic validation errors are rendered under the `syntax-error` category with a redundant doubled prefix 'error[syntax-error] Syntax Error: invalid semantics:'. The message bodies and ranges are good, but the category and redundant prefix read poorly versus rust-analyzer. This is a perva

## cross_file (7 gaps, 25 green)

- **[blocker]** `R/a.R: `first <- 1L`\n R/b.R: `second <- first`\n R/c.R: `#: integer` then `ok <- second``
  - actual: R/c.R: error[type-error] expected `integer`, found `Unknown` on `ok <- second`
  - desired: No diagnostics. `second` exports the scheme of `first` (`integer`), so a third file reading `second` sees `integer`.
  - Core cross-file scheme propagation. A binding whose RHS references a top-level name in ANOTHER file publishes `Unknown` in its exported interface, even though the same reference resolves correctly when consumed inside the SAME file (within_file_resolves_cross_file_read is green). Root cause appears 
- **[blocker]** `R/a.R: `base <- 10L`\n R/b.R: `get_base <- function() base`\n R/c.R: `#: integer` then `r <- get_base()``
  - actual: R/c.R: error[type-error] expected `integer`, found `Unknown` on `r <- get_base()`
  - desired: No diagnostics. `get_base`'s exported return type is `integer` because its body reads package-global `base`.
  - Same root cause, but extremely common in real R packages: a function defined in one file that references a package global (or another function) defined in a different file publishes an `Unknown` return type to the rest of the project. Within-file consumption is green (within_file_resolves_cross_file
- **[blocker]** `R/a.R: `#: fn(n: integer) -> integer` then `helper <- function(n) n + 1L`\n R/b.R: `wrapper <- function(n) helper(n)`\n R/c.R: `#: integer` then `r <- wrapper(2L)``
  - actual: R/c.R: error[type-error] expected `integer`, found `Unknown` on `r <- wrapper(2L)`
  - desired: No diagnostics. `wrapper`'s exported type is `fn(n) -> integer` (calls `helper` from another file).
  - Function-to-function cross-file call chain. `wrapper` exports `Unknown` because its body calls an other-file function. This is the most common cross-file pattern in packages (functions split across files calling each other). Same two-pass interface root cause.
- **[high]** `R/a.R: `#: fn(n: integer) -> integer` then `helper <- function(n) n + 1L`\n R/b.R: `alias <- helper`\n R/c.R: `#: integer` then `r <- alias(2L)``
  - actual: R/c.R: error[type-error] expected `integer`, found `Unknown` on `r <- alias(2L)`
  - desired: No diagnostics. `alias` re-exports `helper`'s scheme.
  - Re-exporting / aliasing another file's binding loses the scheme in the published interface. Same root cause as the binding-to-binding case.
- **[high]** `R/a.R: `second <- first`\n R/b.R: `first <- 1L` then `#: integer` `ok <- second``
  - actual: R/b.R: error[type-error] expected `integer`, found `Unknown` on `ok <- second`
  - desired: No diagnostics. Forward cross-file reference: `second` sees `first`'s scheme even though `first` is defined in a later file.
  - Forward reference across files. `second` in an earlier file references `first` in a later file; per spec forward references across files are allowed and `second` should export `integer`. Currently `Unknown`. Same interface-ordering root cause (made worse by file order, since the dependency is on a l
- **[high]** `R/a.R: `first<-1L`; R/b.R: `second<-first`; R/c.R: `third<-second`; R/d.R: `fourth<-third` then `#: integer` `ok<-fourth``
  - actual: R/d.R: error[type-error] expected `integer`, found `Unknown` on `ok <- fourth`
  - desired: No diagnostics. Deep cross-file chain resolves end to end.
  - Deep reference chain split across four files. Note the analogous chain entirely WITHIN one file (values.R deep_chain_resolves_within_and_across_files) is green, confirming the gap is specifically cross-file interface propagation, not chain depth. A fixpoint/topological interface pass would fix all o
- **[medium]** `R/a.R: `first <- 1L`\n R/b.R: `second <- first`\n scripts/use.R: `#: integer` then `ok <- second``
  - actual: scripts/use.R: error[type-error] expected `integer`, found `Unknown` on `ok <- second`
  - desired: No diagnostics. A script reading a package binding whose type derives from another package file should see the resolved scheme.
  - Script consumer hits the same exported-`Unknown` problem when the package binding it reads (`second`) depends on another package file. Direct script reads of concrete package values/functions are green; only the cross-file-derived case fails. Same root cause; fixing the package interface pass fixes 